package org.firstinspires.ftc.teamcode.util;

/**
 * An enum for defining the starting positions of Skystones during autonomous.
 */
public enum SkystonePosition {
    INNER, CENTER, OUTER, UNKNOWN
}
